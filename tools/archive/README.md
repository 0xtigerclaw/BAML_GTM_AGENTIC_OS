This directory holds code and scripts that are intentionally kept for reference but are no longer part of the active runtime surface.

Current buckets:
- `root-scripts/`: one-off root-level debug, inspection, simulation, and trigger scripts moved out of the project root
- `services/`: retired service implementations kept for reference
- `routes/`: retired debug-only route handlers kept for reference
